/**
 * Testimonials — intentionally not rendered.
 *
 * Nothing on the landing page may claim something that is not true, and there
 * are no real quotes yet. When there are, fill in QUOTES with attributable
 * ones and render <Testimonials /> between the feature grid and the FAQ.
 *
 * Do not populate this with invented names, roles, companies or ratings.
 */

// interface Quote {
//   body: string
//   name: string
//   /** Where they used it — "flatmates in Lahore", "a week in Hunza". */
//   context: string
// }
//
// const QUOTES: Quote[] = []
//
// export function Testimonials() {
//   if (QUOTES.length === 0) return null
//
//   return (
//     <section className="mx-auto max-w-6xl px-4 py-16 sm:px-6">
//       <h2 className="text-center text-2xl font-semibold tracking-tight">
//         What people say
//       </h2>
//       <ul className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
//         {QUOTES.map((quote) => (
//           <li
//             key={quote.name}
//             className="rounded-xl border border-border bg-card p-5 card-shadow"
//           >
//             <blockquote className="text-sm text-foreground">“{quote.body}”</blockquote>
//             <p className="mt-3 text-xs text-muted-foreground">
//               {quote.name} · {quote.context}
//             </p>
//           </li>
//         ))}
//       </ul>
//     </section>
//   )
// }

export {}
